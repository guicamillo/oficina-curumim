---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { getLangFromUrl, useTranslations } from '../../utils/i18n';

const posts = await getCollection('blog', ({ id }) => {
  return id.startsWith('pt-BR/');
});

posts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={t({ key: 'blog.index.meta.title', message: 'Blog - Oficina Curumim' })}>
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="text-center mb-12">
        <h1 class="text-4xl font-bold text-slate-900 mb-4">{t({ key: 'blog.index.title', message: 'Blog' })}</h1>
        <p class="text-xl text-slate-600">{t({ key: 'blog.index.subtitle', message: 'Acompanhe nossas novidades e artigos.' })}</p>
    </div>
    
    <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      {posts.map((post) => (
        <article class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden hover:shadow-md transition-shadow group">
            <div class="h-48 bg-slate-100 relative overflow-hidden">
                {post.data.image ? (
                    <img src={post.data.image} alt={post.data.title} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
                ) : (
                    <div class="w-full h-full flex items-center justify-center bg-gradient-to-br from-white to-gray-400 text-slate-400">
                        <span class="text-4xl">📝</span>
                    </div>
                )}
            </div>
            <div class="p-6">
                <div class="flex items-center gap-2 text-sm text-slate-500 mb-3">
                    <span>{post.data.pubDate.toLocaleDateString('pt-BR')}</span>
                    <span>•</span>
                    <span>{post.data.author}</span>
                </div>
                <h2 class="text-xl font-bold text-slate-900 mb-3 leading-tight">
                    <a href={`/blog/${post.id.replace('pt-BR/', '').replace('.md', '')}`} class="hover:text-blue-600 transition-colors">
                        {post.data.title}
                    </a>
                </h2>
                <p class="text-slate-600 mb-4 line-clamp-3 text-sm">
                    {post.data.description}
                </p>
                <a href={`/blog/${post.id.replace('pt-BR/', '').replace('.md', '')}`} class="inline-flex items-center text-blue-600 font-medium hover:text-blue-700 text-sm">
                    {t({ key: 'blog.index.readMore', message: 'Ler mais' })} <span class="ml-1 group-hover:translate-x-1 transition-transform">&rarr;</span>
                </a>
            </div>
        </article>
      ))}
    </div>
  </main>
</Layout>
